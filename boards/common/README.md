# Common board-package resources

Board packages are declarative and validated against `boards/schema/board.schema.json`.
They may provide constraints, examples, programming metadata, device information,
and documentation. They never contain executable desktop plugins.

