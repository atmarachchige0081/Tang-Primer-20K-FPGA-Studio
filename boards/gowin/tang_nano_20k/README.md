# Tang Nano 20K board package

This initial package supports the on-board 27 MHz clock and LED. Expansion
constraints are intentionally absent because Sipeed board revisions differ;
add them only after checking the matching schematic.

